name: Docker CI

on:
  push:
    # Publish `master` as Docker `latest` image.
    branches:
      - master
      - develop
    # Publish `v1.2.3` tags as releases.
    tags:
      - v*
  # Run tests for any PRs.
  pull_request:

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  GITHUB_CONTEXT: ${{ toJson(github) }}

  IMAGE_NAME: soprun/docker-project
  DOCKER_FILE: ./docker/php/Dockerfile

jobs:
  # Run tests.
  # See also https://docs.docker.com/docker-hub/builds/automated-testing/
  #  test:
  #    runs-on: ubuntu-latest
  #    steps:
  #      - uses: actions/checkout@v2
  #
  #      - name: Run tests
  #        run: |
  #          if [ -f docker-compose.test.yml ]; then
  #            docker-compose --file docker-compose.test.yml build
  #            docker-compose --file docker-compose.test.yml run sut
  #          else
  #            docker build . --file $DOCKER_FILE
  #          fi

  # Push image to GitHub Packages.
  # See also https://docs.docker.com/docker-hub/builds/
  push:
    name: Push image
    # needs:
    #   - test
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    steps:
      - uses: actions/checkout@v2

      - name: Log into registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login docker.pkg.github.com -u ${{ github.actor }} --password-stdin

      - name: Build image
        run: |
          # IMAGE_ID=docker.pkg.github.com/${{ github.repository }}/$IMAGE_NAME

          # Change all uppercase to lowercase
          # IMAGE_ID=$(echo $IMAGE_ID | tr '[A-Z]' '[a-z]')

          IMAGE_TAG_ID=$(echo "${{ github.sha }}")
          IMAGE_TAG_BRANCH=$(echo "${{ github.base_ref }}")

          # Strip git ref prefix from version
          IMAGE_TAG_VERSION=$(echo "${{ github.ref }}" | sed -e 's,.*/\(.*\),\1,')

          # Strip "v" prefix from tag name
          [[ "${{ github.ref }}" == "refs/tags/"* ]] && IMAGE_TAG_VERSION=$(echo $IMAGE_TAG_VERSION | sed -e 's/^v//')

          # Use Docker `latest` tag convention
          [ "$IMAGE_TAG_VERSION" == "master" ] && IMAGE_TAG_VERSION=latest

          echo IMAGE_TAG_ID=$IMAGE_TAG_ID
          echo IMAGE_TAG_BRANCH=$IMAGE_TAG_BRANCH
          echo IMAGE_TAG_VERSION=$IMAGE_TAG

          echo IMAGE_NAME=$IMAGE_NAME
          echo DOCKER_FILE=$DOCKER_FILE
          echo "======================================="
          echo "$GITHUB_CONTEXT"
          # echo ${{ github.event.pull_request.title }}

          echo docker build . \
          --file $DOCKER_FILE \
          --tag "$IMAGE_NAME:$VERSION" \
          --label tag.id=$IMAGE_TAG_ID" \
          --label tag.branch=$IMAGE_TAG_BRANCH \
          --label tag.version=$IMAGE_TAG_VERSION

      - name: Push image
        run: |
          # docker tag $IMAGE_NAME $IMAGE_ID:$VERSION
          # docker push $IMAGE_ID:$VERSION